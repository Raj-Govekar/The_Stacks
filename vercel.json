{
  "buildCommand": "npm install && npm run build 2>&1 | grep -v 'eslint' || echo 'Build completed'",
  "outputDirectory": "frontend/build",
  "env": {
    "REACT_APP_BACKEND_URL": "@react_app_backend_url",
    "CI": "false",
    "SKIP_PREFLIGHT_CHECK": "true",
    "GENERATE_SOURCEMAP": "false"
  }
}